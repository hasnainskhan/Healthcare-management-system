services:
  mongo:
    image: mongo:7
    container_name: smart-healthcare-mongo
    ports:
      - "27017:27017"
    volumes:
      - smart_healthcare_mongo_data:/data/db
    restart: unless-stopped

volumes:
  smart_healthcare_mongo_data:
